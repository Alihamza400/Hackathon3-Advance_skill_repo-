apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-learnflow-app
  labels:
    app.kubernetes.io/name: learnflow-app
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: learnflow-app